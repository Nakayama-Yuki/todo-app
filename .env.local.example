# 環境変数テンプレート
# このファイルをコピーして以下のファイルを作成してください:
# - .env.dev (開発環境用)
# - .env.prod (本番環境用)
# - .env.local (ローカル開発でDockerを使わない場合)

# データベース設定
POSTGRES_DB=todoapp
POSTGRES_USER=todouser
POSTGRES_PASSWORD=your_secure_password_here

# Next.js アプリケーション設定
# 注意: Docker使用時はホスト名を 'postgres' に、ローカル開発時は 'localhost' にしてください
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
NODE_ENV=development

# Docker設定（docker-compose.yml使用時のみ）
APP_COMMAND=pnpm run dev
RESTART_POLICY=no

# 本番環境の場合:
# NODE_ENV=production
# APP_COMMAND=pnpm start
# RESTART_POLICY=unless-stopped
